# AndreyVPN 1.0.5 — Tray Server Switch Build Fix

## Changes

- Updated app version to 1.0.5+72.
- Fixed Windows build failure introduced in 1.0.4 tray server switch.
- Corrected Dart record destructuring for tray proxy key decoding.
- Kept tray server switch logic unchanged.
- No VPN core logic changes.

## Risk

Low. Build-only fix in tray menu click handling.
